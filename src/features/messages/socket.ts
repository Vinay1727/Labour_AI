// Socket.io mock connection
export const connectSocket = () => {
    console.log('Connecting to socket...');
};

export const disconnectSocket = () => {
    console.log('Disconnecting socket...');
};
